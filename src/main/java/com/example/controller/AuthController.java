/*
 * package com.example.controller;
 * 
 * import org.springframework.stereotype.Controller; import
 * org.springframework.ui.Model; import
 * org.springframework.web.bind.annotation.PostMapping;
 * 
 * @Controller public class AuthController {
 * 
 * // Login method
 * 
 * @PostMapping("/login") public String loginUser(String email, String password,
 * Model model) { // Add login logic (validate user, etc.)
 * 
 * // Assuming successful login model.addAttribute("successMessage",
 * "Login successful!");
 * 
 * // Redirect to the login success page return "redirect:/loginSuccess"; //
 * Change this to your success page URL } }
 */